---
"@ionizeio/canvas": patch
---

The Dark Factory vendor script now defaults to the repository's new location, `../Argus` (it moved from `../Dark Factory`), and the repo instructions name that path.
