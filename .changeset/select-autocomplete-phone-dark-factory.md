---
"@ionizeio/canvas": patch
---

On the web, Select, Autocomplete and PhoneInput take Dark Factory's field and menu. The Select trigger and the Autocomplete field are the Input's field: the translucent well at a 10px corner whose hairline turns violet while the list is open, a 13px semibold value (40px tall at base, 34 small, 46 large), the uppercase eyebrow label, and a 14px chevron-down Icon in place of the "▾" text glyph; the Autocomplete helper line takes Dark Factory's small type. A disabled Select or Autocomplete no longer fades: it keeps a hairline frame with no fill and a muted value. Under web glass the Select trigger is the same clear well every web field is, where it used to take the frosted control material. The option lists (the Select's, the Autocomplete's and PhoneInput's countries) are Dark Factory's menu: the panel 8px below the field, 33px rows 2px apart with 12.5px bold labels and the instant hover wash, and the chosen option marked in the selection violet, its label and a checkmark in the gutter every row keeps, with no fill. The Listbox's chosen row reads the same recipe and looks as before. PhoneInput's country segment is the Input's muted addon box with the chevron Icon, its dial code sits at the field's 12px inset, the list's dial codes take the muted menu detail type, and its country rows' pressed and selected fills are inner fills under glass rather than opaque patches. iOS ships no autocomplete control, so the iOS Autocomplete now renders the web's look; its disclosure's touch area reaches 44pt through slop that stops at the text, and its suggestion rows grow to 44pt. Under glass the Android Select and Autocomplete keep their active indicator. iOS keeps its Select pop-up button and PhoneInput with the UIMenu's leading check, and Android its Material 3 exposed dropdowns.
