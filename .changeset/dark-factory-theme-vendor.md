---
"@ionizeio/canvas": patch
---

Vendors Dark Factory's theme source (palettes, type scale, radii, shadows, materials, hue ramp) into `tools/darkfactory/theme.json` with `bun run df:vendor`, and pins the palette facts the coming token mapping relies on. Tooling only; no component changes.
