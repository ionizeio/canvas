---
"@ionizeio/canvas": patch
---

Re-mints the Linux visual baselines for the Checkbox page, whose iOS and Android previews now draw the platform switch for a one-setting Checkbox, and for the FilterPanel page, whose iOS option rows now draw the edit-mode selection circle. No change to the package's behaviour.
