---
"@ionizeio/canvas": patch
---

The text-entry end-to-end checks hold every web field under glass to Dark Factory's clear well (its own tint and hairline, no blur) instead of the removed lens filter. Test fixtures only; the package itself does not change.
