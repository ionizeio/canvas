---
"@ionizeio/canvas": patch
---

The material end-to-end checks now recognize a text field's clear glass well (Dark Factory's unblurred tint and hairline) as the field's own material. Test fixtures only; the package itself does not change.
