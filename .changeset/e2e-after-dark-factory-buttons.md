---
"@ionizeio/canvas": patch
---

Keep two end-to-end checks true after the Dark Factory buttons: the Navbar docs page now fits at 768, so its known-overflow record is gone, and the scrolled-overlay check scrolls the harness page itself instead of relying on it being taller than the viewport above its trigger.
