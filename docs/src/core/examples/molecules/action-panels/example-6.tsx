/* @generated by tools/docgen. DO NOT EDIT. Run `bun run docs:gen`. */
// Source: src/molecules/action-panels/action-panels.md
import type { ExampleScope } from "../../../scope";

export default function Example(scope: ExampleScope) {
  const { Stateful, ActionPanel, AlertDialog, Container } = scope;
  return (
<Stateful initial={false}>
  {(open, setOpen) => (
    <Container md>
      <ActionPanel
        title="Delete this project"
        description="Once you delete a project, there is no going back."
        actionLabel="Delete project"
        destructive
        onAction={() => setOpen(true)}
      />
      <AlertDialog
        open={open}
        onOpenChange={setOpen}
        destructive
        title="Delete this project?"
        description="This permanently removes the project and its data."
        confirmLabel="Delete"
      />
    </Container>
  )}
</Stateful>
  );
}
