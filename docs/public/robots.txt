# Canvas docs. Everything here is public documentation, so crawl all of it.
#
# This file has to exist as a real file. It dates from the single-page export, when
# a _redirects catch-all rewrote every unmatched path to index.html with a 200, so
# without it /robots.txt answered with the HTML shell and a crawler parsed that markup
# as directives: Lighthouse read it as 74 malformed rules. The export is static now
# and a miss is a real 404, but a real file is still the right answer.
#
# No Sitemap line on purpose: there is no sitemap.xml to point at. Add the directive
# in the same commit that starts generating the file.
User-agent: *
Allow: /
